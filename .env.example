# ===========================================
# Variáveis de Ambiente - Futuro Exterminado
# ===========================================

# -------------------------------------------
# API Fogo Cruzado (Obrigatório)
# -------------------------------------------
# Credenciais para autenticação na API do Fogo Cruzado
# Usado em: nuxt.config.ts, utils/scripts/coletar.mjs
FC_EMAIL=seu-email@example.com
FC_PASSWORD=sua-senha-aqui

# -------------------------------------------
# API de Vítimas (Obrigatório)
# -------------------------------------------
# Token para buscar detalhes complementares das vítimas
# Usado em: utils/scripts/coletar.mjs
VICTIMS_TOKEN=seu-token-aqui

# -------------------------------------------
# Configuração da Aplicação (Opcional)
# -------------------------------------------
# Base URL da aplicação (padrão: '/')
# Usado em: nuxt.config.ts
NUXT_APP_BASE_URL=/

# -------------------------------------------
# SMTP - Formulário de Reporte (Opcional)
# -------------------------------------------
# Configurações para envio de e-mails via formulário de reporte
# Usado em: server/api/report.post.ts
# Deixe em branco se não for usar o formulário de reporte

# Servidor SMTP
SMTP_HOST=smtp.seuprovedor.com
SMTP_PORT=465
SMTP_SECURE=true

# Credenciais SMTP
SMTP_USER=no-reply@fogocruzado.org.br
SMTP_PASS=sua-senha-smtp-forte

# E-mails
FROM_EMAIL=no-reply@fogocruzado.org.br
FROM_NAME="Site Fogo Cruzado"
TO_EMAIL=contato@fogocruzado.org.br

# -------------------------------------------
# Mapbox (Opcional)
# -------------------------------------------
# Token de acesso do Mapbox para exibir mapas
# Usado em: plugins/mapbox.client.ts
# Se não configurado, usa o token padrão do Fogo Cruzado
# Recomendado: obter seu próprio token em https://account.mapbox.com/tokens/
NUXT_PUBLIC_MAPBOX_ACCESS_TOKEN=seu-token-mapbox-aqui
